<tr>
        <td><%= link_to (event.who + ": "+ event.type.name), event_path(event) %> - <%= event.time.strftime("%I:%M %p") %>, <%= event.date.strftime("%a, %b %d") %> - <%= event.venue.town %> </td>
</tr>