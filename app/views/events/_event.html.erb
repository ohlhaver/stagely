<% event.who="" if event.who == nil %>
<tr>
        <td><%= link_to (event.who + ": "+ event.description), event_path(event) %> - <%= event.date.strftime("%a, %b %d") %>, <%= event.time.strftime("%I:%M %p") %> - <%= event.venue.town %> </td>
</tr>