<% provide :title, "Lectures, talks, readings, discussions and speeches in New York City" %>
<div class="span8" style="margin-top:-59px">
	<ul class="nav nav-tabs">
	     <% if params[:time] == nil %> 
	     <li class="active"> 
	     <%else%>
	     <li>
	     <%end%>
	    	<a href="events_path">Today</a>
	    </li>
	     <% if params[:time] == "tomorrow" %> 
	     <li class="active"> 
	     <%else%>
	     <li>
	     <%end%>
	    <a href="/?time=tomorrow">Tomorrow</a></li>
	     <% if params[:time] == "week" %> 
	     <li class="active"> 
	     <%else%>
	     <li>
	     <%end%>
	    <a href="/?time=week">This week</a></li>
	     <% if params[:time] == "month" %> 
	     <li class="active"> 
	     <%else%>
	     <li>
	     <%end%>
	    <a href="/?time=month">This month</a></li>
	</ul>
	<table class="table table-striped">
	  <tbody>
	<%= render @events %>
	  </tbody>
	</table>

</div>

<div class="span4">
		<div class="topic_navigation">
		Topics:
		
		<br><br>
		<table class="table table-striped">
	  <tbody>
			<% @words.each do |keyword| %>
			<%= link_to keyword[0] + " (" + keyword[1].to_s + ")", :action => "search", :q => keyword[0], :time => params[:time] %> <br><br>
			<%end%>
	  </tbody>

	</table>
	</div>
</div>



