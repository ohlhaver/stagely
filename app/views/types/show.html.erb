<div class="span8">
<div id ="event_details">
<h3><%= @type.name %></h3>
</div>



<div class="form-actions">
  <%= link_to 'Back', types_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_type_path(@type), :class => 'btn' %>
  <%= link_to 'Delete', type_path(@type), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
</div>